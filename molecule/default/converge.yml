---
- name: Converge
  hosts: all
  vars_files: ../../vars/vault.test.yml
  pre_tasks:
    - name: Role - add user
      include_role:
        name: ../../roles/add-users
  tasks:
    - name: Role - hello-world
      include_role:
        name: ../../roles/hello-world
